# Versions ÉPINGLÉES = celles du déploiement validé (build Railway du 27/07/2026).
# Ne pas repasser en ">=" : fastembed a déjà changé le comportement d'un modèle
# entre deux versions (mean pooling vs CLS) — un déploiement avec une version
# flottante peut dégrader silencieusement la récupération chez un client.

# Serveur web
fastapi==0.140.7
uvicorn==0.51.0
pydantic==2.13.4
slowapi==0.1.10

# Pipeline RAG
chromadb==1.5.9
fastembed==0.8.0

# Lecture des documents
pdfplumber==0.11.10
pypdf==6.14.2
python-docx==1.1.2

# Modèles de langage
anthropic==0.120.0
openai==2.49.0

# Utilitaires
python-dotenv==1.2.2
python-multipart==0.0.32

